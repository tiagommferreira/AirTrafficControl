class Position

types

values

instance variables
	public x: nat;
	public y: nat;
	
operations
	public Position: nat * nat ==> Position
			Position(cordX,cordY) ==
				(
					x := cordX;
					y := cordY;
					return self;
				);

			
functions

traces

end Position